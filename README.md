# DynamicPricing_RNN_Model
This project implements a hybrid RNN model (Conv1D + LSTM) to predict price elasticity in retail environments. Price elasticity measures how demand changes when prices change - a critical metric for dynamic pricing and inventory optimization.
